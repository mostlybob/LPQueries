<Query Kind="Program" />

void Main()
{
    Guid.NewGuid().ToString().Dump();
    Guid.NewGuid().ToString().Dump();
    Guid.NewGuid().ToString().Dump();
    Guid.NewGuid().ToString().Dump();
    Guid.NewGuid().ToString().Dump();
    Guid.NewGuid().ToString().Dump();
}

// Define other methods and classes here
